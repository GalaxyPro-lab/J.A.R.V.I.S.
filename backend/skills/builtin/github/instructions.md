Use the GitHub skill to interact with repositories:
1. Use `github_list_repositories` to find the user's projects.
2. Use `github_get_file_contents` to read code files from specific branches.
3. Use `github_list_issues` and `github_create_issue` to track tasks and bugs.
