When the user asks for real-time information, recent events, technical documentation, or website analysis:
1. Use `web_search` with focused keywords.
2. If a specific URL is provided or found in the search results, use `fetch_web_page` to retrieve full details.
3. Always cite sources with markdown links.
