When the user explicitly asks you to remember something, or provides crucial durable facts:
1. Use `store_memory` with an appropriate level (`global` for personal preferences, `project` for project guidelines).
2. Do not store ephemeral messages or trivial chatter.
3. Use `recall_memory` if context might contain previously saved notes.
